/**
 * Chat — a thin proxy to the existing Python AI service (issue #14). No retrieval, ranking or
 * answer generation lives here on purpose: the plan scopes this module to "chỉ proxy, không viết
 * lại RAG".
 */
package com.cmc.restaurant.chat;
