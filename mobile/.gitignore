.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

# `pubspec.lock` CÓ commit — đây là ứng dụng, không phải thư viện. Bỏ nó ra khỏi kho nghĩa là mỗi
# lần CI chạy lại có thể kéo phiên bản phụ thuộc khác, và một cổng chặn xanh hôm nay không nói
# được gì về bản build ngày mai.

# Rác của IDE do `flutter create` sinh ra.
.idea/
*.iml
